# Agent System

Define the runtime Orchestrator and the minimum specialist agents. For each role document purpose, forbidden actions, typed input, procedure, output envelope, done condition, and fallback.

A runtime handoff exists only when an invocation layer executes it and records input/output references. Project subagents in `.claude/agents/` are development roles and must not be confused with runtime product agents.
