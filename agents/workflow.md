# Workflow Contract

Define legal states and transitions here.

Required branches: missing input, invalid input, tool failure, unsafe output, loop limit, human review, and terminal state. The terminal state must require an explicit human approval event when the project declares a human gate.
